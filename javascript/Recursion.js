class Recursion {

  /**
   * Returns the factorial of <number>
   * @param {Number} number The number to perfrorm the factorial in.
   * Example:
   *    factorial(5) => 120
   * See more examples in the recursion tests.
   */
  factorial(number){

  }

  /**
   * Retrieves the <number> element of the fibonacci series
   * @param {Number} number The position of the fibonacci series to get.
   * Example:
   *  fibonacci(5) => 5
   * See more examples in the recursion tests.
   */
  fibonacci(n){
    
  }

  /**
   * Sums the digits of <number>
   * @param {Number} number The number to sum its digits
   * Example:
   *  sumDigits(123) => 6
   * See more examples in the recursion tests.
   */
  sumDigits(number){
    
  }

  /**
   * Performs the addition of the elements contained in the array
   * Example:
   *   sumArray( [1, 2, 3, 4] ) => 10 :: since 1 + 2 + 3 + 4 = 10;
   * See more examples in the recursion tests.
   */
  sumArray(arrayElements){

  }

  /**
   * Reverses a string content
   * @param {String} string The string to be reversed
   * Example:
   *  reverseString("Hello") => "olleH"
   * More examples available at recursion tests.
   */
  reverseString(string){

  }

  /**
   * Sets the elements of an array in reverse order
   * @param {Array} array The array to reverse
   * Example:
   *   reverseArray([1, 2, 3]) => [3, 2, 1]
   * More examples available at recursion tests.
   */
  reverseArray(array){

  }
}

module.exports = Recursion;
